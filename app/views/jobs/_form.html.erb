
<%= form_for [@project, job] do |f| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>


<div class="field">
   <%= f.label :status %>
   <% Job.statuses.to_a.each do |name, _| %>
     <div class="column small-4">
       <%= f.radio_button :status, name %> <%= name.humanize %>
     </div>
   <% end %>
 </div>

  <div class="actions">
    <%= f.submit class: "button" %>
  </div>
<% end %>
